---
import { format } from "date-fns";
import { utcToZonedTime } from "date-fns-tz";

type Props = {
  date: Date;
};

const { date } = Astro.props;
const localDate = utcToZonedTime(date, "Asia/Tokyo");
---

<time datetime={date.toISOString()}>{format(localDate, "yyyy-MM-dd")}</time>
