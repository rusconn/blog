scalars: &scalars
  Date: string
  DateTime: string

typescript: &typescript
  scalars: *scalars
  skipTypename: true

schema: ${API_URL}
documents:
  - "src/pages/**/*.{ts,tsx}"
  - "src/components/**/*.{ts,tsx}"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      <<: *typescript
